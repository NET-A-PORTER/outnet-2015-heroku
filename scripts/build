#!/usr/bin/env node --harmony

require('../src/global');

var cli = require('commander');
var utils = base.require('core/utils');
var build = base.require('controllers/styles').build;

var styles;

cli
  .arguments('<styles...>')
  .action(function (arg) ***REMOVED***
    styles = arg
***REMOVED***);

cli.parse(process.argv);

if (typeof styles === 'undefined') ***REMOVED***
   console.error('no styles given!');
   process.exit(1);
***REMOVED***

utils.yield(function * main() ***REMOVED***
  var list = yield styles.map(function(style) ***REMOVED***
    return ***REMOVED*** name: style ***REMOVED***
***REMOVED***);
  yield * build(list);
  console.log('Generated', styles);
***REMOVED***);
